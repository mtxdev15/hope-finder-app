---
// /today — the Declare hero. Mounts the React island that ports the proven v1
// Declare flow (struggle -> Worker -> verses/reflection/declarations/prayer) into
// the v3.2 dark-cinematic design. The brain (declare-api) is unchanged from v1.
import DeclareLayout from '../layouts/DeclareLayout.astro';
import DeclareToday from '../app/declare/DeclareToday.jsx';
---
<DeclareLayout
  title="Declare — Today"
  description="Name what's heavy and receive His Word for this moment."
  activeTab="declare"
>
  <main>
    <DeclareToday client:load />
  </main>
</DeclareLayout>
